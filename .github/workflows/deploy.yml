name: Deploy to Netlify

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Netlify build
        run: |
          curl -fsSL -X POST -d '{}' \
            "https://api.netlify.com/build_hooks/6a04d3bd5c5f279511ad740a"
